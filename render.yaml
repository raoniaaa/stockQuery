# Render Blueprint - Frontend
# https://render.com/docs/blueprint-spec

services:
  - type: web
    name: stock-query-frontend
    runtime: docker
    dockerfilePath: ./Dockerfile.frontend
    plan: free
    envVars:
      - key: VITE_API_BASE_URL
        value: "https://stockquery.onrender.com"