# Render Blueprint - Frontend
# https://render.com/docs/blueprint-spec

services:
  - type: web
    name: stock-query-frontend
    runtime: docker
    dockerfilePath: ./Dockerfile.frontend
    plan: free