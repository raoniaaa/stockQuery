server:
  port: ${PORT:8080}

# Supabase Configuration
supabase:
  url: ${SUPABASE_URL}
  key: ${SUPABASE_KEY}

# LLM Configuration (智谱GLM)
llm:
  api-url: https://open.bigmodel.cn/api/paas/v4/chat/completions
  api-key: ${GLM_API_KEY}
  model: GLM-4.7-Flash
